name: Text Analysis Action
description: A GitHub Action that performs simple checks on text
author: samdouble

inputs:
  gh-token:
    description: 'The GitHub token for authentication.'
    required: true
  label:
    description: 'The label to be applied to the pull request.'
    required: true

branding:
  icon: activity
  color: green

runs:
  using: node20
  main: dist/index.js
